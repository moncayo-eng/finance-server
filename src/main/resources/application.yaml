spring:
  application:
    name: finance-server
  datasource:
    url: jdbc:postgresql://localhost:5433/finance
    username: ${USER}
    password:
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false

jwt:
  secret: ${JWT_SECRET:momoney-dev-secret-key-change-in-production-must-be-at-least-256-bits}
  access-token-expiration: 900000
  refresh-token-expiration: 2592000000
